# STM32 工程入口

4x4 矩阵键盘使用 `PD0~PD3` 行和 `PD4~PD7` 列。数字 `1/2/3` 分别发送 `MEASURE 1/2/3`，选择测频相位归零、归零后 `+90°`、归零后二倍频；板载 `KEY3/PE2` 保留为任务 1。STM32 经 USART3 (`PB10/PB11`) 与香橙派通信并执行其返回的 FPGA 控制命令；按键本身不直接改写测频挡位，避免与视觉流程竞争。

LCD 的 `FREQ` 行以 `PI` 标记视觉测频结果。构建、烧录和完整流程见仓库根目录 [README](../README.md)。
