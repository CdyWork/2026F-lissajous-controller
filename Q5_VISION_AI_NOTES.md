# 第五问历史交接说明

本文件保留为历史入口。当前定版方案已合并到仓库根目录 [README](README.md)：

- IMX219 拍摄实时示波器 XY 图像；
- FPGA 使用 `STEP 0..33` 锯齿波表；
- 香橙派采用自适应选挡、q20 背景、同挡位一致性复核；
- STM32 第 4 个按键触发，LCD 显示香橙派返回频率；
- 普通表失败后使用 `6 ms / 10 ms` 与 `2 ms / 10 ms` 低频兜底。

不要依据本文件旧版本中的固定代表挡位、三档融合、八档 `SWEEP` 或历史标定样本修改生产流程。
