# Orange Pi Zero 3W（A733/6GB）+ IMX219
# 视觉识别技术交接手册

**交接目的**：让后续 AI 或工程师可以在不重新处理镜像的前提下，继续完成 IMX219 采集、NPU 推理和 40 FPS 以上视觉识别验证。

**当前边界**：本手册编写后，默认不再重算、改写、校验或重新定制镜像。除非用户另行明确要求，后续工作只在板端和视觉应用代码上进行。

## 1. 目标与环境

### 1.1 目标

- 使用 Orange Pi Zero 3W A733 6GB 与 IMX219 MIPI 摄像头。
- 通过手机热点联网，SSH 无人值守登录。
- 采用 A733 NPU 做目标检测，目标是端到端持续 **40 FPS 以上**。
- 先保证采集和裸推理性能，再接入显示、录像或网络输出。

### 1.2 已确认硬件

- SoC：全志 A733。
- CPU：2×Cortex-A76 + 6×Cortex-A55，最高约 2 GHz。
- NPU：约 3 TOPS；6 GB 内存不是当前主要瓶颈。
- 摄像头：官方资料当前主要支持 IMX219。
- 摄像头接口：22-pin、0.5 mm MIPI-CSI；IMX219 常用 22-pin 转 15-pin 排线。
- 板上有两个 4-lane MIPI-CSI 接口；CAM1 通常映射到 `/dev/video0`，CAM2 通常映射到 `/dev/video8`，以实机枚举结果为准。

### 1.3 已选系统

推荐并已验证的系统是 **Debian Bullseye Server，Linux 5.15.147**：

```text
Orangepizero3w_1.0.0_debian_bullseye_server_linux5.15.147.img
```

原因：现有资料对 Bullseye 的 GStreamer、V4L2、GPU/视频和 NPU 组合支持最完整。Bookworm 镜像不作为本项目当前主系统。

资料位置：

```text
F:\orangedata\OrangePi_Zero3W_A733_用户手册_v1.0.pdf
F:\orangedata\原理图\OPI ZERO 3W V1_2_原理图.pdf
```

重点页：用户手册第 8-9 页（芯片/NPU/CSI）、第 51-53 页（镜像与驱动）、第 172-176 页（IMX219/GStreamer/OpenCV）、第 185-188 页（NPU/YOLOv5）；原理图第 13 页（MIPI CSI）。

## 2. 当前已完成状态

以下状态已经在实机上确认，不应当当作“仅配置文件推测”：

| 项目 | 已确认结果 |
|---|---|
| 主机名 | `orangepi-vision` |
| 最近一次 DHCP 地址 | `192.168.242.224`（手机热点 DHCP 地址，可能变化） |
| SSH | `active`，可直接使用主机别名登录 |
| Wi-Fi | 已连接手机热点；热点凭据已写入设备，但不在本文记录 |
| 网络管理 | `NetworkManager: active` |
| 服务发现 | `avahi-daemon: active` |
| OpenCV | `4.5.1` |
| GStreamer | `1.18.4` |
| 视频节点 | `/dev/video0` 已发现 |
| 摄像头驱动 | `imx219`、`vin_v4l2` 已加载/可见 |
| 首次安装 | 已完成；日志应位于 `/var/log/orangepi-firstboot.log` |

### 2.1 SSH 登录

Windows 端已配置专用 SSH 主机别名，首选：

```powershell
ssh orangepi-vision
```

如果主机名解析暂时不可用，可使用最近一次地址临时验证：

```powershell
ssh orangepi@192.168.242.224
```

热点重连后地址可能改变；不要把 DHCP 地址硬编码到应用中。优先使用 SSH 配置别名或 Avahi 主机名解析。

SSH 使用公钥认证，设备端已关闭密码登录和 root 远程登录。私钥存放在 Windows 用户 SSH 目录中，本文不复制其内容、不在脚本中打印其内容。

## 3. 关键命令与验证

### 3.1 板端基础状态

```bash
hostnamectl
ip -br addr
systemctl is-active NetworkManager ssh avahi-daemon
cat /etc/os-release
uname -a
```

### 3.2 摄像头和 V4L2

```bash
ls -l /dev/video*
v4l2-ctl --list-devices
v4l2-ctl -d /dev/video0 --all
v4l2-ctl -d /dev/video0 --list-formats-ext
lsmod | grep -E 'imx219|vin_v4l2'
```

先以 `--list-formats-ext` 的真实输出为准，不要假定每个分辨率都支持 60 FPS。常见检查方式：

```bash
v4l2-ctl -d /dev/video0 \
  --set-fmt-video=width=1280,height=720,pixelformat=NV12 \
  --set-parm=60 \
  --stream-mmap=3 --stream-count=300 --stream-to=/dev/null
```

该命令用于采集吞吐测试；若格式或帧率不被驱动接受，应改用枚举结果中的格式。测试结束后查看命令输出和实际耗时，换算采集 FPS。

### 3.3 GStreamer 采集检查

先查看节点能否被 GStreamer 打开：

```bash
gst-launch-1.0 -v v4l2src device=/dev/video0 num-buffers=120 \
  ! video/x-raw,width=1280,height=720,framerate=60/1 \
  ! fakesink sync=false
```

如果摄像头只提供 NV12 或其他格式，应在能力枚举后调整 caps。最终应用优先采用 V4L2 MMAP/DMABUF，减少用户态复制；GStreamer 主要用于快速验证管线。

### 3.4 软件版本

```bash
python3 -c 'import cv2; print(cv2.__version__)'
gst-launch-1.0 --version
pkg-config --modversion opencv4 2>/dev/null || true
```

首次安装日志：

```bash
sudo tail -n 100 /var/log/orangepi-firstboot.log
test -f /var/lib/orangepi-provision.done && echo provision-done
```

## 4. 40 FPS 以上推荐架构

推荐数据流：

```text
IMX219 1280x720@60
  -> V4L2 MMAP/DMABUF（环形缓冲）
  -> NV12 直接缩放/letterbox
  -> YOLOv5n 320x320 INT8 NBG
  -> A733 NPU/VIPLite 推理
  -> CPU 后处理/NMS
  -> 异步显示或网络输出
```

### 4.1 采集参数

- 首选 `1280x720@60`；若该模式在当前 IMX219 模组上不可用，退回驱动实际支持的高帧率模式。
- 1080p 通常只有 30 FPS，不能作为“真实 40 FPS”采集源。
- 预处理优先 NV12/YUV，避免每帧 `NV12 -> BGR -> RGB` 的重复转换。
- 推理输入建议 320×320；确认精度后再尝试 416×416。输入越大，NPU和内存带宽压力越高。

### 4.2 缓冲与线程

- 采集、预处理、NPU 推理、后处理/输出分线程或异步任务。
- 队列长度保持 1-2，采用“最新帧优先”；不要让延迟随队列无限增长。
- 使用固定大小的缓冲池，避免每帧分配和释放内存。
- 关闭显示和录像先测裸链路；显示/编码应为独立线程，不能阻塞采集。
- Python 适合验证设备和算法，不建议作为最终 40 FPS 主循环；最终实现优先 C/C++，或 Python 调用已编译的 NPU/预处理模块。

### 4.3 性能预算

40 FPS 的端到端帧预算是不超过 **25 ms/帧**。建议验收门槛：

1. 采集阶段：持续至少 55 FPS。
2. NPU 单独推理：持续至少 50 FPS。
3. 采集+预处理+推理+后处理、无显示：持续至少 45 FPS。
4. 接入实际显示/网络输出后：持续至少 40 FPS，且无持续丢帧和队列堆积。

每阶段都应记录平均 FPS、P1/P99 延迟、丢帧数、CPU/NPU 占用和温度。只看瞬时 FPS 不算通过。

## 5. 后续实现步骤

1. 在板端执行 `v4l2-ctl --list-formats-ext -d /dev/video0`，确认 IMX219 的真实高帧率模式。
2. 用 V4L2 MMAP 或 DMABUF 完成 300 帧采集基准，保存 FPS 和丢帧统计。
3. 建立 NV12 到 320×320 letterbox 的零拷贝/少拷贝预处理基准。
4. 准备 YOLOv5n（或同级轻量模型）的 INT8 校准数据，转换为 A733 NPU 可加载的 NBG 格式。
5. 用厂商 NPU/VIPLite API 完成单线程推理，并单独测量推理延迟。
6. 加入异步队列、CPU 后处理和丢旧帧策略，测无显示全流程。
7. 最后接入显示、编码或网络发送，重复 40 FPS 验收。
8. 固定测试场景至少运行 10 分钟，记录温度、降频、内存和错误日志。

## 6. 已知风险、未验证项与排障

### 6.1 已知风险

- 手机热点 DHCP 地址会变化；SSH 别名或 Avahi 可能比固定 IP 可靠。
- IMX219 模组、排线方向、CAM1/CAM2 选择会影响节点和模式；排线金属触点方向必须按板卡丝印和手册确认，不能仅凭颜色判断。
- 具体摄像头模式依赖模组时钟、驱动和光照配置；`1280x720@60` 是目标，不是对所有模组的保证。
- NPU SDK、NBG 转换器和量化工具的版本必须与 A733 系统/驱动匹配；当前只确认系统侧 NPU 能力，尚未完成最终模型转换和精度评测。
- 40 FPS 受散热、DDR 带宽、格式转换和输出路径影响；6 GB 内存本身不能保证帧率。

### 6.2 摄像头无节点

```bash
dmesg -T | grep -Ei 'imx219|vin|csi|mipi|firmware|error'
lsmod | grep -E 'imx219|vin_v4l2'
sudo modprobe vin_v4l2
v4l2-ctl --list-devices
```

检查：断电后重新插排线；确认 22-pin/15-pin 规格和触点方向；更换 CAM1/CAM2；确认供电和排线没有松动。

### 6.3 有节点但打不开或帧率低

```bash
v4l2-ctl -d /dev/video0 --all
v4l2-ctl -d /dev/video0 --list-formats-ext
media-ctl -p 2>/dev/null || true
```

先使用驱动列出的分辨率、像素格式和帧率组合；关闭其他占用摄像头的进程；避免同时运行预览和基准测试。

### 6.4 SSH 或热点异常

```bash
systemctl status NetworkManager ssh avahi-daemon --no-pager
nmcli device status
nmcli connection show --active
ip route
```

确认手机热点允许设备间通信、热点没有达到客户端上限，并检查板端时间和 DHCP 租约。不要在交接记录中追加热点密码。

### 6.5 性能不达标

按“采集 -> 预处理 -> NPU -> 后处理 -> 输出”逐段隔离。每次只增加一个阶段；如果无显示全流程已低于 45 FPS，先优化像素格式、缩放和 NPU 输入尺寸，不要先调显示刷新率。

## 7. 本任务重要文件

### 7.1 输出文件

```text
C:\Users\Administrator\Documents\Codex\2026-07-31\f-orangedata\outputs\OrangepiZero3w_1.0.0_bullseye_server_vision_autosetup.img
C:\Users\Administrator\Documents\Codex\2026-07-31\f-orangedata\outputs\OrangepiZero3w_1.0.0_bullseye_server_vision_autosetup.img.sha256.txt
```

这两个文件是本任务已产生的镜像及其记录，仅作为背景和回滚材料；本交接阶段不再修改它们。

### 7.2 工作脚本

```text
C:\Users\Administrator\Documents\Codex\2026-07-31\f-orangedata\work\provision\customize-orangepi.sh
C:\Users\Administrator\Documents\Codex\2026-07-31\f-orangedata\work\provision\verify-custom-image.sh
C:\Users\Administrator\Documents\Codex\2026-07-31\f-orangedata\work\provision\inspect-image.sh
C:\Users\Administrator\Documents\Codex\2026-07-31\f-orangedata\work\provision\inspect-account.sh
C:\Users\Administrator\Documents\Codex\2026-07-31\f-orangedata\work\provision\fix-live-source.sh
```

脚本中曾使用当前镜像对应的分区参数 `offset=33554432`、`size=3393191936`。它们只对当时镜像验证过；以后若重新处理其他镜像，必须先动态读取 MBR 分区，不能盲目复用。

## 8. 给接手 AI 的明确下一步清单

- [ ] 不修改镜像；先通过 `ssh orangepi-vision` 登录板端。
- [ ] 重新记录当前 DHCP 地址、温度、内核、服务状态和摄像头节点。
- [ ] 保存 `v4l2-ctl --list-formats-ext` 的完整输出，确定最高稳定帧率模式。
- [ ] 完成 300 帧 V4L2 采集基准，确认采集阶段达到 55 FPS 以上。
- [ ] 选择 NV12 等驱动实际支持的格式，测量缩放/letterbox 的耗时和内存复制次数。
- [ ] 获取与 A733 匹配的 NPU/VIPLite SDK 和 NBG 转换链，完成 YOLOv5n INT8 模型转换。
- [ ] 记录单独 NPU 推理延迟和 FPS，目标至少 50 FPS。
- [ ] 实现异步采集、推理、后处理，队列限制为 1-2 帧，先完成无显示全流程。
- [ ] 按 10 分钟稳定运行标准验收 40 FPS，并记录丢帧、P99 延迟、温度和降频。
- [ ] 最后再接入显示/录像/网络输出，确保输出不会阻塞采集线程。

**交接结论**：板端联网、SSH、OpenCV、GStreamer、V4L2 以及 IMX219/VIN 驱动已经验证通过；剩余核心工作是依据实际模式完成高帧率采集基准、A733 NPU 模型转换和端到端性能验收。
