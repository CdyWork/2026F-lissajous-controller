# 第五问历史仿真

此目录是早期锯齿波视觉识别原型，用于验证波形几何和图像算法，不参与当前硬件测频。

生产方案使用 FPGA `STEP 0..33` 表、香橙派常驻服务、IMX219 实时 XY 拍摄、自适应选挡和 STM32 LCD 回传。完整说明见仓库根目录 [README](../README.md)。

保留本目录仅供回归仿真或研究参考；其中的 10 ms/1 ms 波形、双斜坡逻辑和全频率离线回归不能用于当前设备的按键测频流程。
